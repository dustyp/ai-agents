#!/bin/bash
# claude-agent.sh - A wrapper script to fully bootstrap agents with Claude

claude "CRITICAL STARTUP INSTRUCTIONS: You must follow the bootstrap instructions in CLAUDE.md. If you cannot find your memory tool module then when you are done with the other tasks go back and try to search for the knowledge graph in your memory tool to gain additinal context"
